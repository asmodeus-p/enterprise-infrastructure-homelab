# Lessons Learned

## Networking

VirtualBox networking configuration is just as important as server configuration.

## SSH

A running SSH service is useless if routing or networking is incorrect.

## Linux

Ubuntu Server is intentionally minimal, requiring comfort with command-line administration.
